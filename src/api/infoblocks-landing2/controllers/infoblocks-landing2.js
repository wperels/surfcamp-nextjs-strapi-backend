'use strict';

/**
 * infoblocks-landing2 controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::infoblocks-landing2.infoblocks-landing2');
