'use strict';

/**
 * infoblocks-landing2 router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::infoblocks-landing2.infoblocks-landing2');
