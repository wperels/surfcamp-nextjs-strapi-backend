'use strict';

/**
 * infoblocks-landing2 service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::infoblocks-landing2.infoblocks-landing2');
